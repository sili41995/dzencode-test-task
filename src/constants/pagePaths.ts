enum PagePaths {
  homePath = '/',
  orders = '/orders',
  products = '/products',
  groups = '/groups',
  settings = '/settings',
  users = '/users',
  dynamicParam = 'id',
}

export default PagePaths;
