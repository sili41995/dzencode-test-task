const enum DateFormats {
  monthNumber = 'MM',
  fullDate = 'dd / MMM / yyyy',
  guaranteeDate = 'dd / MM / yyyy',
  dayOfWeek = 'EEEE',
  clockDate = 'dd MMM, yyyy',
  clockTime = 'kk:mm',
}

export default DateFormats;
